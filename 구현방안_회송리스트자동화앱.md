# 물류센터 회송리스트 자동화 앱 — 구현방안 (v0.1 draft)

작성자: 물류담당자(쿠팡풀필먼트서비스 3PL 센터) 요청 · 작성일: 2026-08-19
목적: 사내 공용 개발자 검토 및 피드백용 초안. 본 문서는 확정 스펙이 아니며, 하단 "열린 질문" 항목에 대한 답변을 받아 세부 개발 착수 전 v1.0으로 확정 예정.

## 1. 배경과 목표

물류센터 내 회송대상 상품(오류신고, 초과보관 등으로 반품/폐기 대상이 된 상품)을 관리하는 회송리스트가 현재 구글 스프레드시트에 수기 또는 반수기로 기록되고 있는 것으로 파악된다. 본 프로젝트의 목표는 PDA와 휴대폰의 바코드 스캔 기능을 앱과 연동하여, 작업자가 스캔만으로 회송리스트를 실시간 자동 업데이트하도록 만드는 것이다. 사용자는 단기 아르바이트 인력을 포함한 현장 작업자이므로, 별도 교육 없이도 직관적으로 사용할 수 있어야 한다는 제약이 가장 중요한 설계 기준이다.

## 2. 전체 아키텍처

세 가지 요구사항 — (1) 휴대폰 카메라와 PDA 내장 스캐너를 하나의 앱에서 함께 지원, (2) 개발·서버 연동이 간편할 것, (3) 별도 계정 발급 없는 간이 인증 — 을 종합하면, 아래와 같은 구조를 제안한다.

**클라이언트: PWA(Progressive Web App) 단일 코드베이스**
설치형 네이티브 앱 대신 PWA로 개발하면 앱스토어 심사·사내 MDM 배포 없이 URL 공유만으로 즉시 사용할 수 있어, 입퇴사가 잦은 단기 인력 운영 환경에 적합하다. 무엇보다 PWA 방식은 아래의 하이브리드 스캔 방식을 하나의 코드베이스로 구현 가능하게 하는 전제가 된다.

**하이브리드 바코드 스캔**
- 휴대폰: 브라우저 카메라 API(getUserMedia)로 영상을 받아 바코드 인식 라이브러리(예: ZXing-js, 혹은 Chrome이 지원하는 경우 BarcodeDetector Web API)로 프레임 내 바코드를 실시간 디코딩한다. 별도 서버 처리 없이 클라이언트에서 즉시 인식된다.
- PDA: 대부분의 산업용 Android PDA(질문하신 'XRG1 IPR 스캐너' 포함, 다만 실제 모델의 설정은 확인 필요— 8번 열린 질문 참고)는 내장 스캐너를 **키보드 에뮬레이션(Keyboard Wedge/HID) 모드**로 동작시킬 수 있다. 이 모드에서는 스캐너가 인식한 바코드 값이 마치 키보드로 타이핑된 것처럼 현재 포커스된 입력창에 자동 입력되고 Enter(또는 Tab)가 뒤따라 전송된다. 이 경우 네이티브 SDK 연동 없이, 화면의 스캔 입력창에 포커스를 유지한 상태로 `keydown`/`input` 이벤트만 감지하면 되므로 PWA 구조를 그대로 유지할 수 있다.

이 방식의 장점은 네이티브 앱 개발 없이 하나의 웹 코드베이스로 두 기기를 모두 지원할 수 있다는 것이다. 단, PDA가 실제로 키보드 에뮬레이션 모드를 지원하는지, 스캔 후 종료 문자(Enter/Tab)가 붙는지는 실기 확인이 필요하다(열린 질문 참고). 만약 해당 PDA가 키보드 에뮬레이션을 지원하지 않고 자체 SDK(Intent 브로드캐스트 등)로만 스캔 값을 전달한다면, PDA용 얇은 WebView 래퍼 앱(Android, 수 시간 내 개발 가능한 수준)을 추가하여 SDK로 받은 값을 WebView의 PWA 화면에 주입하는 절충안을 검토한다.

**백엔드: Google Apps Script 웹 앱**
서버 구축 없이 구글시트와 직접 연동되고, 무료로 운영 가능하며, 이미 사용 중인 시트를 그대로 데이터 저장소로 활용할 수 있어 "개발 편의성과 서버 연동 용이성"이라는 요청 기준에 가장 부합한다. 시트에 Apps Script를 바인딩하고 `doPost(e)` 함수로 웹 API처럼 동작시킨다. 사진 업로드(폐기차량 사진)는 `DriveApp`으로 Google Drive에 저장 후 파일 URL을 시트에 기록하는 방식을 사용한다.

다만 Apps Script는 실행 할당량이 있다. Google 공식 문서 기준으로 일반(Gmail) 계정은 하루 URL Fetch 호출 20,000회, 트리거 총 실행시간 90분/일, 1회 실행 최대 6분의 제한이 있으며, Google Workspace 계정은 URL Fetch 5배(100,000회/일), 트리거 총 실행시간 4배(6시간/일)로 완화된다([Google Developers 공식 문서](https://developers.google.com/apps-script/guides/services/quotas)). 롯데글로벌로지스 사내 구글 계정이 Workspace라면 이 한도로 충분할 것으로 판단되나, 향후 다수 센터로 확대 시에는 동시 실행 한도(consumer 30건)도 함께 고려해 별도 서버(Cloud Run 등 + 서비스 계정 Sheets API) 전환을 로드맵 3단계로 남겨둔다.

**인증: 간이 인증**
로그인 화면에서 작업자명과 VF ID를 수기 입력만 받고 별도 계정 검증은 하지 않는다. 다만 Apps Script 웹 앱은 배포 시 "URL을 아는 모든 사용자"에게 열려 있으므로, 요청 헤더 또는 payload에 고정 앱 토큰(간단한 문자열 키)을 포함시켜 앱 외부에서의 무단 POST 요청을 최소한으로 걸러내는 것을 권장한다. 이는 사용자 인증이 아닌 "이 앱을 통해서만 호출되었는지"를 확인하는 최소한의 안전장치다.

## 3. 화면 구성 및 사용자 흐름

1. **로그인 화면**: 상단 타이틀 'XRG1 IPR 스캐너', 작업자명 · VF ID 입력, [시작하기] 버튼.
2. **홈 화면**: 상단 헤더(타이틀 + 우측 홈/로그아웃 아이콘), 작업자 정보 배지, [폐기] [회송] [조회] 3개의 카드형 버튼, 하단 '롯데글로벌로지스' 워터마크 고정 표시.
3. **폐기 화면**: (1단계) 폐기차량 사진 촬영 + 차량번호/접안시간/발차시간 입력 → (2단계) 회송상품 IPR바코드 다건 스캔(스캔 리스트에 실시간 누적, 개별 삭제 가능) → (3단계) [폐기처리 완료] 클릭 시 시트의 해당 IPR바코드 행에 처리일과 'D+6 폐기'를 기록.
4. **회송 화면**: [업체회송] / [택배회송] 선택.
   - 업체회송: 차량정보 입력 → IPR바코드 다건 스캔 → [회송처리 완료] 클릭 시 처리일 / '업체 트럭 회송' 기록.
   - 택배회송: IPR바코드-운송장번호를 쌍으로 다건 스캔(IPR바코드 스캔 → 해당 운송장번호 스캔이 이어지는 2단계 반복) → [회송처리 완료] 클릭 시 처리일 / '택배회송' / 운송장번호 기록.
5. **조회 화면**: IPR바코드 1건 스캔 시 상품바코드 · 오류신고일 · 벤더명 · 수량 · 회수구분(택배/업체직접회수) · D+6 초과 여부를 카드로 즉시 표시. 이때 화면 상단에는 스캔한 IPR바코드를, 본문에는 참고용 상품바코드를 구분하여 표시한다.

각 스캔 단계에서는 스캔값이 시트의 IPR바코드 열과 매칭되는지 즉시 조회하여, 매칭 실패 시 "미등록 IPR바코드"임을 그 자리에서 알려 오처리를 방지한다.

## 4. 데이터 모델 (Google Sheet: `회송리스트`)

**중요한 수정사항**: 작업자가 현장에서 실제로 스캔하는 값은 **IPR바코드**이며, 조회 화면에 표시되는 **상품바코드**는 이와 별개의 참고용 식별 코드다. 즉 앱의 모든 스캔 동작(폐기·회송·조회)은 IPR바코드를 매칭 키로 사용하고, 상품바코드는 매칭에는 관여하지 않는 표시 전용 필드로 취급해야 한다. 이는 최초 설계안 대비 변경된 부분이며, 이번 수정으로 시트에도 IPR바코드 열이 매칭 키로 별도 추가되었다.

| 열 | 필드명 | 설명 | 기입 시점 |
|---|---|---|---|
| A | 오류신고일 | 최초 이슈 등록일 (YYYY-MM-DD) | 기존 프로세스에서 사전 기입 |
| B | **IPR바코드** | **스캔 매칭 기준 키(고유값). 작업자가 실제로 스캔하는 값** | 기존 프로세스에서 사전 기입 |
| C | 상품바코드 | 상품 식별용 참고 코드(스캔 대상 아님, 조회 화면 표시용) | 기존 프로세스에서 사전 기입 |
| D | 벤더명 | 상품 벤더사명 | 기존 프로세스에서 사전 기입 |
| E | 수량 | 대상 수량 | 기존 프로세스에서 사전 기입 |
| F | 처리구분 | 미처리 / D+6 폐기 / 업체 트럭 회송 / 택배회송 | 앱에서 처리 시 자동 기입 |
| G | 처리일 | 처리 완료 시각 | 앱에서 자동 기입 |
| H | 운송장번호 | 택배회송 건에 한해 기입 | 앱에서 자동 기입 |
| I | 처리자 / VF ID | 처리한 작업자 정보 | 앱에서 자동 기입 |
| J | 차량번호 / 접안시간 / 발차시간 | 폐기·업체회송 건에 한해 기입 | 앱에서 자동 기입 |
| K | 차량사진 URL | 폐기 건, Drive 업로드 링크 | 앱에서 자동 기입 |
| L | D+6 초과여부 | `=IF(TODAY()-A2>6,"초과","미초과")` 수식으로 자동 계산 | 시트 수식(실시간) |

열 이름과 순서는 기존 시트 구조에 맞춰 조정 가능하며, 위 표는 하나의 제안안이다. 특히 IPR바코드와 상품바코드가 1:1로 대응되는지(상품 1개당 IPR바코드 1개), 혹은 하나의 상품바코드에 여러 IPR바코드가 매칭될 수 있는지는 실제 현장 라벨링 규칙 확인이 필요하다(8장 열린 질문 참고).

## 5. API 명세 (Apps Script `doPost`)

모든 요청은 `{ "action": "...", "payload": {...}, "token": "앱토큰" }` 형태의 JSON을 POST하고, 응답은 `{ "success": true/false, ... }` 형태로 반환한다. Apps Script Web App은 `fetch`에서 `Content-Type: application/json`으로 보낼 경우 브라우저 preflight(OPTIONS) 요청을 Apps Script가 처리하지 못해 CORS 오류가 발생하는 경우가 흔히 보고되는 문제이므로, `Content-Type: text/plain;charset=utf-8`로 보내고 Apps Script 측에서 `JSON.parse(e.postData.contents)`로 파싱하는 방식을 권장한다.

모든 요청/응답의 바코드 파라미터는 `iprBarcode`로 명명한다(실제 스캔값). 조회 결과에는 참고용으로 `productBarcode`(상품바코드)를 별도 필드로 함께 반환한다.

**iprBarcodeLookup** — 스캔 즉시 매칭 확인(폐기/회송 화면에서 리스트에 추가하기 전 검증용)
요청: `{ "iprBarcode": "IPR2308050112" }`
응답: `{ "success": true, "found": true, "vendor": "OO상사", "qty": 3, "reportDate": "2026-08-10" }`

**inquiry** — 조회 화면 전용 상세 조회
요청: `{ "iprBarcode": "IPR2308050112" }`
응답: `{ "success": true, "found": true, "iprBarcode": "IPR2308050112", "productBarcode": "8801234567890", "reportDate": "2026-08-10", "vendor": "OO상사", "qty": 3, "method": "업체직접회수", "isOverDPlus6": true }`

**processDiscard** — 폐기 처리 완료
요청: `{ "worker": {"name":"홍길동","vfId":"VF1234"}, "vehicle": {"plateNo":"12가3456","arrivalTime":"09:10","departureTime":"09:40"}, "photoBase64": "...", "iprBarcodes": ["IPR2308050112","IPR2308010078"] }`
응답: `{ "success": true, "processedCount": 2, "failedBarcodes": [] }`

**processReturnVendor** — 업체회송 처리 완료
요청: `{ "worker": {...}, "vehicle": {...}, "iprBarcodes": [...] }`
응답: 위와 동일 형태

**processReturnParcel** — 택배회송 처리 완료
요청: `{ "worker": {...}, "items": [{"iprBarcode":"IPR2308160045","trackingNo":"6001..."}, ...] }`
응답: 위와 동일 형태

## 6. 리스크 및 대응방안

PDA의 키보드 에뮬레이션 모드 지원 여부가 가장 큰 기술적 불확실성이므로 착수 전 실기 테스트가 선행되어야 한다. Apps Script 동시 실행/일일 할당량은 소규모 단일 센터 운영에서는 여유가 있으나 다수 센터로 확대 시 병목이 될 수 있어 로드맵상 확장 지점으로 명시했다. 사진 업로드는 base64 전송 시 용량이 커질 수 있어 클라이언트에서 리사이즈(예: 1280px 폭 제한) 후 전송하는 처리가 필요하다. 오프라인/네트워크 불안정 구간(지하 물류센터 등)을 고려해, 스캔 리스트는 완료 버튼을 누르기 전까지 클라이언트 로컬 상태로 보관하고, 완료 시점에만 일괄 전송하는 구조로 네트워크 단절 영향을 최소화한다.

## 7. 개발 로드맵 (제안)

1단계 — 디자인/구현방안 피드백 확정, PDA 키보드 에뮬레이션 모드 실기 검증
2단계 — Apps Script 백엔드 + PWA 프론트엔드 MVP 개발(폐기/회송/조회 3 화면), 단일 센터 파일럿
3단계 — 파일럿 피드백 반영, 필요 시 서버 아키텍처(Cloud Run + Sheets API)로 전환 검토, 타 센터 확대

## 8. 열린 질문 (피드백 요청)

IPR바코드와 상품바코드의 실제 매핑 관계(1:1 여부, 발번 주체와 발번 시점)를 확인해야 본 문서 4장의 데이터 모델을 확정할 수 있다. 실제 사용 중인 PDA(XRG1 IPR 스캐너)가 키보드 에뮬레이션(HID) 출력 모드를 지원하는지, 스캔 후 종료 문자로 Enter가 자동 전송되는지 확인이 필요하다. 회사 구글 계정이 Workspace 계정인지 여부(할당량 산정에 영향)와, 구글시트/Drive에 접근 가능한 서비스 계정 또는 Apps Script 배포 권한을 담당자가 발급할 수 있는지 확인이 필요하다. 기존 회송리스트 시트의 실제 컬럼 구조를 공유받아 본 문서 4장의 제안 구조와 맞춰볼 필요가 있다. 마지막으로, 앱 토큰 방식의 최소 인증 수준이 사내 보안 정책상 허용되는지 확인이 필요하다.
