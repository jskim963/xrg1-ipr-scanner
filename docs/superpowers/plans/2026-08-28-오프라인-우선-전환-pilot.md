# 오프라인 우선 전환 (v3.2) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 스캔/조회/처리를 항상 로컬(IndexedDB)만 쓰도록 바꾸고, 서버 통신은 [데이터 새로고침]/[지금 동기화] 버튼을 눌렀을 때만 발생하게 한다.

**Architecture:** `frontend/js/sync.js`의 `inquiry`/`process*` 함수들에서 온라인 시도(`api.*` 호출 후 실패 시 큐잉)를 제거하고 항상 로컬 경로로 직행시킨다. `syncDown`/`syncUp`만 남는 API 표면에 맞춰 `api.js`/`api-payloads.js`/`mock-data.js`/백엔드의 개별 실시간 엔드포인트를 삭제한다. 스캔 화면에 "마지막 새로고침" 표시를 추가한다.

**Tech Stack:** Vanilla JS ES modules(frontend), Google Apps Script CommonJS 스타일(appsscript), Node `node:test`.

**참고 스펙:** `docs/superpowers/specs/2026-08-26-회송리스트-자동화-앱-design.md` 11장

---

### Task 1: `formatRelativeMinutes` 순수 함수 추가 (TDD)

**Files:**
- Modify: `frontend/js/lib/format.js`
- Test: `frontend/js/lib/format.test.js`

- [ ] **Step 1: 실패하는 테스트 작성**

`frontend/js/lib/format.test.js` 파일 끝에 추가:

```javascript
test('formatRelativeMinutes: 1분 미만이면 방금 전', () => {
  const now = new Date('2026-08-28T09:00:30.000Z').getTime();
  assert.equal(formatRelativeMinutes('2026-08-28T09:00:00.000Z', now), '방금 전');
});

test('formatRelativeMinutes: 60분 미만이면 N분 전', () => {
  const now = new Date('2026-08-28T09:05:00.000Z').getTime();
  assert.equal(formatRelativeMinutes('2026-08-28T09:00:00.000Z', now), '5분 전');
});

test('formatRelativeMinutes: 24시간 미만이면 N시간 전', () => {
  const now = new Date('2026-08-28T12:00:00.000Z').getTime();
  assert.equal(formatRelativeMinutes('2026-08-28T09:00:00.000Z', now), '3시간 전');
});

test('formatRelativeMinutes: 24시간 이상이면 N일 전', () => {
  const now = new Date('2026-08-30T09:00:00.000Z').getTime();
  assert.equal(formatRelativeMinutes('2026-08-28T09:00:00.000Z', now), '2일 전');
});

test('formatRelativeMinutes: isoString이 없으면 null', () => {
  assert.equal(formatRelativeMinutes(null, Date.now()), null);
  assert.equal(formatRelativeMinutes('', Date.now()), null);
});
```

`format.test.js` 최상단 import 줄도 함께 수정:

```javascript
import { formatDateDisplay, formatDPlus6Badge, formatMethodLabel, splitBarcodeSuffix, formatRelativeMinutes } from './format.js';
```

- [ ] **Step 2: 테스트 실패 확인**

```bash
cd frontend && npm test
```
예상: `formatRelativeMinutes is not defined` 관련 실패.

- [ ] **Step 3: 구현**

`frontend/js/lib/format.js` 파일 끝에 추가:

```javascript
export function formatRelativeMinutes(isoString, nowMs) {
  if (!isoString) return null;
  var now = nowMs == null ? Date.now() : nowMs;
  var diffMs = now - new Date(isoString).getTime();
  var minutes = Math.floor(diffMs / 60000);
  if (minutes < 1) return '방금 전';
  if (minutes < 60) return minutes + '분 전';
  var hours = Math.floor(minutes / 60);
  if (hours < 24) return hours + '시간 전';
  var days = Math.floor(hours / 24);
  return days + '일 전';
}
```

- [ ] **Step 4: 테스트 통과 확인**

```bash
cd frontend && npm test
```
예상: 전체 PASS (기존 43개 + 신규 5개 = 48개).

- [ ] **Step 5: 커밋**

```bash
git add frontend/js/lib/format.js frontend/js/lib/format.test.js
git commit -m "feat: add formatRelativeMinutes for last-sync display"
```

---

### Task 2: `sync.js`를 오프라인 우선으로 재작성

**Files:**
- Modify: `frontend/js/sync.js` (전체 재작성)

이 파일은 Node 테스트가 없는 orchestration 레이어다(기존 관례 유지, 브라우저 수동 검증). TDD 대상 아님 — Task 1에서 만든 순수 함수와 이미 테스트된 `offline-logic.js`를 그대로 재사용해 안전하게 재배선한다.

- [ ] **Step 1: 전체 내용을 아래로 교체**

`frontend/js/sync.js`:

```javascript
import * as api from './api.js';
import * as db from './lib/offline-db.js';
import { buildOfflineInquiryResult, buildSyncQueueItem, summarizeSyncResults } from './lib/offline-logic.js';

var LAST_SYNC_KEY = 'xrg1-last-sync-at';

export function downloadSnapshot() {
  return api.syncDown().then(function (res) {
    if (!res.success) throw new Error('스냅샷 다운로드 실패');
    return db.saveSnapshot(res.items).then(function () {
      try {
        localStorage.setItem(LAST_SYNC_KEY, new Date().toISOString());
      } catch (err) {
        console.warn('마지막 동기화 시각 저장 실패:', err);
      }
    });
  });
}

export function getLastSyncedAt() {
  try {
    return localStorage.getItem(LAST_SYNC_KEY);
  } catch (err) {
    return null;
  }
}

export function inquiry(iprBarcode) {
  return Promise.all([db.getSnapshot(), db.getQueueItems()]).then(function (results) {
    return buildOfflineInquiryResult(results[0], results[1], iprBarcode);
  });
}

function queueAndSucceed_(action, params) {
  var item = buildSyncQueueItem(action, Object.assign({ now: new Date() }, params));
  return db.addQueueItem(item).then(function () {
    return { success: true };
  }).catch(function (err) {
    console.warn('로컬 큐 저장 실패:', err);
    return { success: false, error: 'OFFLINE_QUEUE_FAILED' };
  });
}

export function processDiscard(iprBarcode, worker, photoBase64) {
  return queueAndSucceed_('processDiscard', { iprBarcode: iprBarcode, worker: worker, photoBase64: photoBase64 });
}

export function processReturnVendor(iprBarcode, worker) {
  return queueAndSucceed_('processReturnVendor', { iprBarcode: iprBarcode, worker: worker });
}

export function processReturnParcel(iprBarcode, trackingNo, worker) {
  return queueAndSucceed_('processReturnParcel', { iprBarcode: iprBarcode, worker: worker, trackingNo: trackingNo });
}

export function processReturnZoneMove(iprBarcode, worker) {
  return queueAndSucceed_('processReturnZoneMove', { iprBarcode: iprBarcode, worker: worker });
}

export function getPendingCount() {
  return db.getQueueCount();
}

export function syncNow() {
  return db.getQueueItems().then(function (items) {
    if (items.length === 0) {
      return { appliedCount: 0, duplicateCount: 0, notFoundCount: 0, errorCount: 0 };
    }
    return api.syncUp(items).then(function (res) {
      if (!res.success) throw new Error('동기화 실패');
      var results = res.results || [];
      var resolvedIprs = {};
      results.forEach(function (r) {
        if (r.status === 'applied' || r.status === 'duplicate' || r.status === 'not_found') {
          resolvedIprs[r.iprBarcode] = true;
        }
        // status === 'error'는 큐에 남겨 다음 동기화 시도 때 재시도한다.
      });
      var remaining = items.filter(function (item) { return !resolvedIprs[item.iprBarcode]; });
      return db.replaceQueue(remaining).then(function () {
        return summarizeSyncResults(results);
      });
    });
  });
}
```

- [ ] **Step 2: 기존 테스트가 깨지지 않는지 확인**

```bash
cd frontend && npm test
```
예상: 전체 PASS (sync.js는 테스트 대상 아니라 이 변경으로 실패할 테스트 없음).

- [ ] **Step 3: 커밋**

```bash
git add frontend/js/sync.js
git commit -m "feat: make sync.js offline-first (no online attempt on scan/process)"
```

---

### Task 3: 로그인 시 자동 다운로드 제거

**Files:**
- Modify: `frontend/js/screens/login.js`

- [ ] **Step 1: 자동 downloadSnapshot 호출 삭제**

`frontend/js/screens/login.js`의 클릭 핸들러를 아래와 같이 바꾼다(전체 파일):

```javascript
export function renderLogin(root, ctx) {
  root.innerHTML =
    '<div class="card login-card">' +
    '  <h1>XRG1 IPR 스캐너</h1>' +
    '  <label>작업자명<input id="loginName" type="text" autocomplete="off" /></label>' +
    '  <label>VF ID<input id="loginVfId" type="text" autocomplete="off" /></label>' +
    '  <button id="loginSubmit" class="btn btn-primary" type="button">시작하기</button>' +
    '  <p id="loginError" class="error-text"></p>' +
    '</div>';

  root.querySelector('#loginSubmit').addEventListener('click', function () {
    var name = root.querySelector('#loginName').value.trim();
    var vfId = root.querySelector('#loginVfId').value.trim();
    if (!name || !vfId) {
      root.querySelector('#loginError').textContent = '작업자명과 VF ID를 모두 입력해주세요.';
      return;
    }
    ctx.dispatch({ type: 'LOGIN_SUCCESS', worker: { name: name, vfId: vfId } });
  });
}
```

- [ ] **Step 2: 테스트 확인**

```bash
cd frontend && npm test
```
예상: 전체 PASS (login.js는 테스트 대상 아님).

- [ ] **Step 3: 커밋**

```bash
git add frontend/js/screens/login.js
git commit -m "fix: remove automatic snapshot download on login"
```

---

### Task 4: 스캔 화면 - 마지막 새로고침 표시 + pendingCount 버그 수정

**Files:**
- Modify: `frontend/js/screens/scan.js`

- [ ] **Step 1: import 줄에 `formatRelativeMinutes` 추가**

`frontend/js/screens/scan.js` 1~3행을 아래로 교체:

```javascript
import { attachHidScanner } from '../scanner.js';
import { formatDateDisplay, formatDPlus6Badge, formatMethodLabel, splitBarcodeSuffix, formatRelativeMinutes } from '../lib/format.js';
import { escapeHtml } from '../lib/html.js';
```

- [ ] **Step 2: sync-bar에 마지막 새로고침 span 추가**

`renderScan` 안의 `root.innerHTML` 블록에서 아래 부분:

```javascript
    '  <div class="sync-bar">' +
    '    <span id="pendingCount">동기화 대기: 확인 중...</span>' +
    '    <button id="refreshSnapshotBtn" class="btn btn-secondary" type="button">데이터 새로고침</button>' +
    '    <button id="syncNowBtn" class="btn btn-secondary" type="button">지금 동기화</button>' +
    '  </div>' +
```

을 아래로 교체:

```javascript
    '  <div class="sync-bar">' +
    '    <span id="pendingCount">동기화 대기: 확인 중...</span>' +
    '    <span id="lastSyncedAt">' + escapeHtml(formatLastSyncedLabel_(ctx.sync.getLastSyncedAt())) + '</span>' +
    '    <button id="refreshSnapshotBtn" class="btn btn-secondary" type="button">데이터 새로고침</button>' +
    '    <button id="syncNowBtn" class="btn btn-secondary" type="button">지금 동기화</button>' +
    '  </div>' +
```

- [ ] **Step 3: `refreshPendingCount_`에 `.catch()` 추가 (버그 수정)**

기존:

```javascript
function refreshPendingCount_(root, ctx) {
  var requestId = ++latestPendingCountRequestId_;
  ctx.sync.getPendingCount().then(function (count) {
    if (requestId !== latestPendingCountRequestId_) return;
    var el = root.querySelector('#pendingCount');
    if (el) el.textContent = '동기화 대기: ' + count + '건';
  });
}
```

을 아래로 교체:

```javascript
function refreshPendingCount_(root, ctx) {
  var requestId = ++latestPendingCountRequestId_;
  ctx.sync.getPendingCount().then(function (count) {
    if (requestId !== latestPendingCountRequestId_) return;
    var el = root.querySelector('#pendingCount');
    if (el) el.textContent = '동기화 대기: ' + count + '건';
  }).catch(function () {
    if (requestId !== latestPendingCountRequestId_) return;
    var el = root.querySelector('#pendingCount');
    if (el) el.textContent = '동기화 대기: 확인 실패';
  });
}

function formatLastSyncedLabel_(lastSyncedIso) {
  var rel = formatRelativeMinutes(lastSyncedIso, Date.now());
  return rel ? '마지막 새로고침: ' + rel : '새로고침 필요';
}
```

(두 함수를 이 위치에 나란히 추가한다. `formatLastSyncedLabel_`은 Step 2에서 이미 참조하고 있다.)

- [ ] **Step 4: 브라우저 수동 확인**

```bash
cd frontend && npx --yes serve -l 5173 .
```
`http://localhost:5173` 접속 → 로그인 → 스캔 화면 상단에 "새로고침 필요"가 보이는지 확인 → [데이터 새로고침] 클릭 후 화면이 다시 그려지며 "마지막 새로고침: 방금 전"으로 바뀌는지 확인.

- [ ] **Step 5: 테스트 확인 후 커밋**

```bash
cd frontend && npm test
git add frontend/js/screens/scan.js
git commit -m "feat: show last-refresh time on scan screen, fix stuck pendingCount on error"
```

---

### Task 5: 처리 완료 메시지 문구 고정화 (discard/returnParcel/returnVendor)

**Files:**
- Modify: `frontend/js/screens/discard.js:42`
- Modify: `frontend/js/screens/returnParcel.js:23`
- Modify: `frontend/js/screens/returnVendor.js:41`

`res.offline`이 이제 항상 정의되지 않으므로(Task 2에서 `queueAndSucceed_`가 `{success:true}`만 반환) 조건문을 고정 문구로 바꾼다.

- [ ] **Step 1: `discard.js:42`**

```javascript
        var suffix = res.offline ? ' (오프라인 처리 — 동기화 대기 중)' : '';
```
을
```javascript
        var suffix = ' (동기화 대기 중 — 아직 서버에 반영되지 않음)';
```
로 교체.

- [ ] **Step 2: `returnParcel.js:23`**

```javascript
        var suffix = res.offline ? ' (오프라인 처리 — 동기화 대기 중)' : '';
```
을
```javascript
        var suffix = ' (동기화 대기 중 — 아직 서버에 반영되지 않음)';
```
로 교체.

- [ ] **Step 3: `returnVendor.js:41`**

```javascript
              var suffix = res.offline ? ' (오프라인 처리 — 동기화 대기 중)' : '';
```
을
```javascript
              var suffix = ' (동기화 대기 중 — 아직 서버에 반영되지 않음)';
```
로 교체. (이 파일은 들여쓰기가 한 단 더 깊으니 원본 들여쓰기를 유지할 것.)

- [ ] **Step 4: 테스트 확인 후 커밋**

```bash
cd frontend && npm test
git add frontend/js/screens/discard.js frontend/js/screens/returnParcel.js frontend/js/screens/returnVendor.js
git commit -m "fix: use connectivity-agnostic wording for queued-processing message"
```

---

### Task 6: `api.js`에서 개별 실시간 액션 제거 (real config 취급 주의)

**Files:**
- Modify: `frontend/js/api.js`

**중요:** 이 파일은 로컬에 실배포용 real config(`MOCK_MODE=false`, 실제 `API_BASE_URL`)가 **커밋되지 않은 상태**로 남아있다. 이 태스크는 반드시 아래 순서를 지킨다: (1) 현재 실제 값을 기록 → (2) placeholder로 교체한 채로 코드 수정 → (3) placeholder 상태로 커밋 → (4) 커밋 직후 즉시 real 값으로 복원(재스테이징/재커밋 금지, 그냥 파일만 원복).

- [ ] **Step 1: 현재 uncommitted 상태 확인 및 real 값 기록**

```bash
git diff frontend/js/api.js
```
`MOCK_MODE`와 `API_BASE_URL`의 현재(real) 값을 그대로 메모해둔다(보통 `MOCK_MODE = false`와 실제 배포 URL).

- [ ] **Step 2: 파일 전체를 아래로 교체 (placeholder 값 포함)**

```javascript
import { mockCall } from './lib/mock-data.js';

export var MOCK_MODE = true; // 실배포 전 false로 바꾸고 아래 URL을 실제 배포 URL로 교체
export var API_BASE_URL = 'https://script.google.com/macros/s/REPLACE_WITH_DEPLOYMENT_ID/exec';

function callApi(action, payload) {
  if (MOCK_MODE) return mockCall(action, payload);
  return fetch(API_BASE_URL, {
    method: 'POST',
    headers: { 'Content-Type': 'text/plain;charset=utf-8' },
    body: JSON.stringify(Object.assign({ action: action }, payload))
  }).then(function (res) { return res.json(); });
}

export function syncDown() {
  return callApi('syncDown', {});
}
export function syncUp(items) {
  return callApi('syncUp', { items: items });
}
```

(기존 `import { buildInquiryPayload, ... } from './lib/api-payloads.js';`와 `inquiry`/`processDiscard`/`processReturnVendor`/`processReturnParcel`/`processReturnZoneMove` export를 전부 제거했다.)

- [ ] **Step 3: 커밋 (placeholder 상태로)**

```bash
git add frontend/js/api.js
git commit -m "refactor: api.js only exposes syncDown/syncUp (individual real-time actions removed)"
```

- [ ] **Step 4: real 값으로 즉시 복원 (재스테이징/재커밋 금지)**

`frontend/js/api.js`의 `MOCK_MODE`/`API_BASE_URL` 두 줄만 Step 1에서 기록해둔 real 값으로 되돌린다. 이 변경은 커밋하지 않고 working tree에 uncommitted 상태로 남긴다.

```bash
git status --short frontend/js/api.js
```
예상 출력: ` M frontend/js/api.js` (unstaged 상태로만 남아야 함)

---

### Task 7: `api-payloads.js`와 테스트 삭제

**Files:**
- Delete: `frontend/js/lib/api-payloads.js`
- Delete: `frontend/js/lib/api-payloads.test.js`

Task 6에서 이 파일들을 쓰던 유일한 소비자(`api.js`의 개별 액션 함수들)를 제거했으므로, 이 두 파일을 참조하는 곳이 없다.

- [ ] **Step 1: 다른 참조가 없는지 확인**

```bash
grep -rn "api-payloads" frontend/js --include=*.js
```
예상: 아무 결과 없음 (자기 자신 두 파일 제외하면 매치 없음 — 이미 삭제 대상이므로 결과가 나와도 두 파일 자신뿐이어야 한다).

- [ ] **Step 2: 삭제 및 커밋**

```bash
git rm frontend/js/lib/api-payloads.js frontend/js/lib/api-payloads.test.js
cd frontend && npm test
```
예상: 전체 PASS (제거된 파일에 있던 테스트만큼 테스트 개수가 줄어듦).

```bash
git commit -m "chore: remove unused api-payloads (only individual real-time actions used it)"
```

---

### Task 8: `mock-data.js` 정리

**Files:**
- Modify: `frontend/js/lib/mock-data.js`

- [ ] **Step 1: `mockCall`에서 `inquiry` 분기 제거, 기본 응답을 UNKNOWN_ACTION으로 변경**

`frontend/js/lib/mock-data.js`의 `mockCall` 함수를 아래로 교체:

```javascript
export function mockCall(action, payload) {
  return new Promise(function (resolve) {
    setTimeout(function () {
      if (action === 'syncDown') {
        var items = Object.keys(MOCK_ROWS)
          .map(function (key) { return MOCK_ROWS[key]; })
          .filter(function (r) { return r.found && !r.duplicate && !r.alreadyProcessed; });
        resolve({ success: true, items: items });
        return;
      }
      if (action === 'syncUp') {
        var results = (payload.items || []).map(function (item) {
          return { iprBarcode: item.iprBarcode, status: 'applied' };
        });
        resolve({ success: true, results: results });
        return;
      }
      resolve({ success: false, error: 'UNKNOWN_ACTION' });
    }, 200);
  });
}
```

(파일 상단의 `MOCK_ROWS` 상수는 `syncDown` 분기가 계속 쓰므로 그대로 둔다.)

- [ ] **Step 2: 테스트 확인 후 커밋**

```bash
cd frontend && npm test
git add frontend/js/lib/mock-data.js
git commit -m "chore: trim mockCall to syncDown/syncUp only, match backend UNKNOWN_ACTION shape"
```

---

### Task 9: `Handlers.gs`에서 개별 실시간 핸들러 삭제

**Files:**
- Modify: `appsscript/Handlers.gs`

`handleInquiry`, `handleProcessDiscard`, `handleProcessReturnVendor`, `handleProcessReturnParcel`, `handleProcessReturnZoneMove` 5개 함수를 삭제한다. `applySyncItem_`과 `handleSyncUp`, `handleSyncDown`, `jsonResponse_`는 그대로 둔다(이 함수들이 쓰는 `Logic.js`의 `buildProcessUpdate`/`buildStagingUpdate`/`ACTION_TYPE`/`buildInquiryResult` 등도 변경 없음).

- [ ] **Step 1: 5개 함수 삭제**

`appsscript/Handlers.gs`에서 `handleInquiry` 함수 전체(파일 최상단 `jsonResponse_` 바로 다음)부터 `handleProcessReturnZoneMove` 함수 끝(`handleSyncDown` 시작 직전)까지, 즉 아래 5개 함수 전체를 삭제한다:
- `function handleInquiry(payload) { ... }`
- `function handleProcessDiscard(payload) { ... }`
- `function handleProcessReturnVendor(payload) { ... }`
- `function handleProcessReturnParcel(payload) { ... }`
- `function handleProcessReturnZoneMove(payload) { ... }`

삭제 후 파일은 `jsonResponse_` 함수 바로 다음에 `handleSyncDown` 함수가 오도록 만든다.

- [ ] **Step 2: appsscript 테스트 확인**

```bash
cd appsscript && npm test
```
예상: 전체 PASS, 23개 그대로 (이 함수들은 원래 Node 테스트 대상이 아니었음 — `Handlers.gs`는 impure라 테스트 없음).

- [ ] **Step 3: 커밋**

```bash
git add appsscript/Handlers.gs
git commit -m "chore: remove unused real-time process/inquiry handlers (syncUp/syncDown only remain)"
```

---

### Task 10: `Code.gs` 라우터 정리

**Files:**
- Modify: `appsscript/Code.gs`

- [ ] **Step 1: doPost 라우터에서 5개 case 삭제**

`appsscript/Code.gs` 전체를 아래로 교체:

```javascript
function doPost(e) {
  var result;
  try {
    var payload = JSON.parse(e.postData.contents);
    switch (payload.action) {
      case 'syncDown':
        result = handleSyncDown(payload);
        break;
      case 'syncUp':
        result = handleSyncUp(payload);
        break;
      default:
        result = { success: false, error: 'UNKNOWN_ACTION' };
    }
  } catch (err) {
    Logger.log('doPost error: ' + err + '\n' + (err && err.stack));
    result = { success: false, error: 'SERVER_ERROR', message: String(err) };
  }
  return jsonResponse_(result);
}
```

- [ ] **Step 2: appsscript 테스트 확인 후 커밋**

```bash
cd appsscript && npm test
git add appsscript/Code.gs
git commit -m "chore: trim doPost router to syncDown/syncUp only"
```

---

### Task 11: 로컬 테스트 가이드 갱신

**Files:**
- Modify: `docs/로컬-테스트-가이드.md`

- [ ] **Step 1: 2절 마지막 확인 문구 수정**

기존:
```
8. 다시 `cd frontend && npx --yes serve -l 5173 .`로 서버를 띄우고, 테스트 시트에 넣어둔 IPR바코드로 스캔 → 조회 → 처리까지 실제 시트가 업데이트되는지 확인합니다.
```
을
```
8. 다시 `cd frontend && npx --yes serve -l 5173 .`로 서버를 띄우고, 스캔 화면에서 [데이터 새로고침]을 먼저 누른 뒤(v3.2부터 로그인 시 자동 다운로드하지 않음), 테스트 시트에 넣어둔 IPR바코드로 스캔 → 조회 → 처리 → [지금 동기화]까지 실제 시트가 업데이트되는지 확인합니다.
```
로 교체.

- [ ] **Step 2: 3절("오프라인 동작 확인하기") 전체 교체**

기존 3절 전체를:

```
## 3. 로컬 우선 동작 확인하기 (v3.2)

1. Mock 모드 또는 실제 배포 모드로 앱을 로그인한다(v3.2부터 로그인 시 자동 다운로드하지 않는다 — 화면 상단에 "새로고침 필요"가 보인다).
2. [데이터 새로고침]을 눌러 미처리 건을 기기에 저장한다. 성공하면 "마지막 새로고침: 방금 전"으로 바뀐다.
3. (선택) 브라우저 개발자도구의 네트워크 탭에서 "오프라인" 체크박스를 켜거나 기기의 Wi-Fi를 꺼본다 — 온라인/오프라인 상관없이 스캔·조회·처리 동작이 동일한지 확인한다(v3.2부터 항상 로컬만 사용).
4. 방금 다운로드된(=아직 처리 안 된) IPR바코드를 조회하면 로컬 스냅샷에서 찾아 평소와 동일한 카드가 표시된다.
5. 스냅샷에 없는(예: 원래 미등록이거나 이미 처리된) IPR바코드를 조회하면 "재확인 필요(오프라인 상태)"가 표시된다.
6. 폐기/회송 처리를 완료하면 "(동기화 대기 중 — 아직 서버에 반영되지 않음)" 문구와 함께 완료되고, 화면 상단 "동기화 대기: N건" 숫자가 올라간다.
7. 네트워크가 연결된 상태에서 [지금 동기화] 버튼을 누르면 큐에 쌓인 건이 실제 시트에 반영되고, 결과 요약(반영/중복/미확인 건수)이 표시된다.
8. 두 기기(또는 브라우저 탭 두 개)로 같은 IPR바코드를 각각 처리한 뒤 순서대로 동기화해보면, 먼저 동기화된 값이 시트에 남고 나중 것은 AC열("중복/재신고 점검")에 "중복스캔"이 추가되는지 확인할 수 있다.
```

로 교체.

- [ ] **Step 3: 커밋**

```bash
git add "docs/로컬-테스트-가이드.md"
git commit -m "docs: update local testing guide for v3.2 offline-first behavior"
```

---

## 완료 후 확인

- [ ] `cd frontend && npm test` 전체 PASS
- [ ] `cd appsscript && npm test` 전체 PASS
- [ ] `git status --short` 결과가 `M frontend/js/api.js` (real config, uncommitted) 하나만 남는지 확인
- [ ] 브라우저(Mock 모드)로 로그인 → 새로고침 → 스캔 → 처리 → 동기화 전체 흐름 1회 수동 확인
