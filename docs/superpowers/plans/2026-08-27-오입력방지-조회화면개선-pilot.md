# 오입력 방지 및 조회 화면 개선 (v3.1) Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 처리 화면에서 조회 결과를 잃지 않고 이전 화면으로 돌아갈 수 있게 하고, 조회 카드에 IPR바코드를 추가 표시하며 상품바코드 뒷 4자리를 강조하고, 업체직접회수를 "회송존 이동"/"회수 완료" 두 갈래로 나눠 각각 재확인 후 처리하도록 만든다.

**Architecture:** 상태 리듀서에 화면을 지정해서 이동하되 조회 결과는 지우지 않는 `GO_BACK` 이벤트를 추가한다. 업체직접회수 화면은 기존 단일 완료 버튼 대신 두 버튼(각각 `window.confirm()` 재확인 후 처리)으로 바뀐다. "회송존 이동"은 U/X열만 기입하는 새 액션(`processReturnZoneMove`)으로, 기존 폐기/회송 액션과 동일하게 온라인 우선·오프라인 큐잉을 지원한다.

**Tech Stack:** 기존과 동일(Vanilla JS ES 모듈, Google Apps Script).

**설계 근거:** `docs/superpowers/specs/2026-08-26-회송리스트-자동화-앱-design.md` 10장 (v3.1)

---

## 파일 구조 개요

```
/appsscript
  Logic.js       (수정: COLUMNS.PROGRESS_STATUS, ACTION_TYPE.RETURN_ZONE_MOVE, buildStagingUpdate) — TDD
  Sheet.gs        (수정: writeStagingResult_ 추가) — 수동 검증
  Handlers.gs     (수정: handleProcessReturnZoneMove 추가, applySyncItem_ 확장) — 수동 검증
  Code.gs         (수정: processReturnZoneMove 라우팅) — 수동 검증

/frontend/js
  lib/
    format.js + format.test.js           (수정: splitBarcodeSuffix 추가) — TDD
    api-payloads.js + api-payloads.test.js (수정: buildReturnZoneMovePayload 추가) — TDD
    state.js + state.test.js               (수정: RETURN_VENDOR_CONFIRM→RETURN_VENDOR_CHOICE 개명, GO_BACK 이벤트 추가) — TDD
  api.js           (수정: processReturnZoneMove 추가 — MOCK_MODE/URL 로컬 설정 보존 필요)
  sync.js           (수정: processReturnZoneMove 오프라인 대응 추가)
  screens/
    scan.js          (수정: 조회 카드에 IPR바코드 표시 + 상품바코드 뒷 4자리 강조)
    discard.js        (수정: 뒤로가기로 전환)
    returnMethodChoice.js (수정: 뒤로가기로 전환)
    returnParcel.js       (수정: 뒤로가기로 전환)
    returnVendor.js         (전면 재작성: 회송존 이동/회수 완료 2분기 + 재확인)
  app.js            (수정: SCREEN.RETURN_VENDOR_CHOICE 라우팅)
  css/style.css      (수정: .barcode-highlight 추가)

/docs
  로컬-테스트-가이드.md (수정: 이번 변경사항 테스트 절 추가)
```

---

### Task 1: Apps Script — 컬럼 상수/액션 타입/스테이징 업데이트 빌더 (TDD)

**Files:**
- Modify: `appsscript/Logic.js`
- Modify: `appsscript/Logic.test.js`

- [ ] **Step 1: 실패하는 테스트 추가**

`appsscript/Logic.test.js`에 추가(파일 상단 `require` 구조분해 목록에 `ACTION_TYPE`를 추가해야 한다 — 없다면 추가할 것):
```js
const { COLUMNS, ACTION_TYPE, determineReturnRoute, buildInquiryResult, buildProcessUpdate, buildLogRow, FINAL_STATUS_LABEL, findMatchingIndexesInColumn, buildStagingUpdate } = require('./Logic.js');

test('COLUMNS.PROGRESS_STATUS는 X열(0-based 23)이다', () => {
  assert.equal(COLUMNS.PROGRESS_STATUS, 23);
});

test('ACTION_TYPE.RETURN_ZONE_MOVE는 processReturnZoneMove이다', () => {
  assert.equal(ACTION_TYPE.RETURN_ZONE_MOVE, 'processReturnZoneMove');
});

test('buildStagingUpdate: 처리일과 회송존 이동 문자열을 담는다', () => {
  const now = new Date('2026-08-27T09:00:00Z');
  const update = buildStagingUpdate(now);
  assert.equal(update.remarkDate, now);
  assert.equal(update.progressStatus, '회송존 이동');
});
```
주의: 이 파일 최상단에 이미 `const { COLUMNS, findMatchingIndexesInColumn, determineReturnRoute, buildInquiryResult, buildProcessUpdate, buildLogRow, FINAL_STATUS_LABEL } = require('./Logic.js');` 형태의 구조분해 import가 있을 것이다. 새 이름(`ACTION_TYPE`, `buildStagingUpdate`)만 그 목록에 추가하고 나머지는 그대로 둔다. 위 3개 테스트만 파일 끝에 추가한다.

- [ ] **Step 2: 테스트 실행하여 실패 확인**

Run: `cd appsscript && npm test`
Expected: `COLUMNS.PROGRESS_STATUS`가 `undefined`, `buildStagingUpdate is not a function` 등으로 FAIL

- [ ] **Step 3: 구현 추가**

`appsscript/Logic.js`를 열어 `COLUMNS` 객체에서 `TRACKING_NO: 22,` 다음 줄에 추가(기존 `DUPLICATE_CHECK: 28` 줄은 그대로 유지):
```js
  PROGRESS_STATUS: 23,
```

`ACTION_TYPE` 객체(`DISCARD`, `RETURN_VENDOR`, `RETURN_PARCEL`를 담고 있는 객체)에 항목 추가:
```js
  RETURN_ZONE_MOVE: 'processReturnZoneMove'
```

`buildProcessUpdate` 함수 아래, `module.exports` 위에 새 함수 추가:
```js
function buildStagingUpdate(now) {
  return { remarkDate: now, progressStatus: '회송존 이동' };
}
```

`module.exports` 객체에 `buildStagingUpdate: buildStagingUpdate,` 추가.

- [ ] **Step 4: 테스트 실행하여 통과 확인**

Run: `cd appsscript && npm test`
Expected: `# fail 0`

- [ ] **Step 5: Commit**

```bash
git add appsscript/Logic.js appsscript/Logic.test.js
git commit -m "feat(appsscript): add progress-status column, zone-move action type, staging update builder"
```

---

### Task 2: Apps Script — 스테이징 결과 쓰기 (Sheet.gs)

**Files:**
- Modify: `appsscript/Sheet.gs`

Apps Script 전용 전역을 쓰므로 Node 테스트 대상이 아니다.

- [ ] **Step 1: writeStagingResult_ 추가**

`appsscript/Sheet.gs`를 열어, `writeProcessResult_` 함수 바로 아래(그리고 `appendLogRow_`/`markDuplicateScan_` 위 어디든)에 추가:
```js
// "회송존 이동" 처리 전용: U열(Remark 날짜)에 처리일, X열(택배 진행상태 — 이 플로우에서는
// 재사용)에 '회송존 이동' 문자열을 기입한다. S/T열(최종 처리)은 건드리지 않는다 — 아직
// 최종 처리가 아닌 중간 상태이기 때문이다.
function writeStagingResult_(rowIndex, update) {
  var sheet = getReturnListSheet_();
  var sheetRow = rowIndex + 2;
  sheet.getRange(sheetRow, COLUMNS.REMARK_DATE + 1).setValue(update.remarkDate);
  sheet.getRange(sheetRow, COLUMNS.PROGRESS_STATUS + 1).setValue(update.progressStatus);
}
```

- [ ] **Step 2: Commit**

```bash
git add appsscript/Sheet.gs
git commit -m "feat(appsscript): add staging-result write for zone-move flow"
```

---

### Task 3: Apps Script — processReturnZoneMove 핸들러 + applySyncItem_ 확장 + 라우팅

**Files:**
- Modify: `appsscript/Handlers.gs`
- Modify: `appsscript/Code.gs`

- [ ] **Step 1: handleProcessReturnZoneMove 추가**

`appsscript/Handlers.gs`를 열어, `handleProcessReturnParcel` 함수 바로 아래(그리고 `handleSyncDown` 위)에 추가:
```js
function handleProcessReturnZoneMove(payload) {
  var lock = LockService.getScriptLock();
  lock.waitLock(10000); // 10초 대기
  try {
    var iprColumn = readIprColumnValues_();
    var matches = findMatchingIndexesInColumn(iprColumn, payload.iprBarcode);
    if (matches.length !== 1) {
      return { success: false, error: matches.length === 0 ? 'NOT_FOUND' : 'DUPLICATE' };
    }
    var now = new Date();
    var update = buildStagingUpdate(now);
    writeStagingResult_(matches[0], update);
    appendLogRow_(buildLogRow({
      timestamp: now,
      statusLabel: '회송존 이동',
      iprBarcode: payload.iprBarcode,
      workerName: payload.worker && payload.worker.name,
      vfId: payload.worker && payload.worker.vfId
    }));
    return { success: true };
  } finally {
    lock.releaseLock();
  }
}
```

- [ ] **Step 2: applySyncItem_ 확장**

`appsscript/Handlers.gs`를 열어 현재 `applySyncItem_` 함수 전체를 읽고, 다음 두 부분을 수정한다(다른 부분은 그대로 유지):

(a) 함수 맨 앞의 `actionType` 3항 연산자 체인에 `processReturnZoneMove` 분기를 추가한다. 현재:
```js
  var actionType = item.action === 'processDiscard' ? ACTION_TYPE.DISCARD
    : item.action === 'processReturnVendor' ? ACTION_TYPE.RETURN_VENDOR
    : item.action === 'processReturnParcel' ? ACTION_TYPE.RETURN_PARCEL
    : null;
```
다음으로 교체:
```js
  var actionType = item.action === 'processDiscard' ? ACTION_TYPE.DISCARD
    : item.action === 'processReturnVendor' ? ACTION_TYPE.RETURN_VENDOR
    : item.action === 'processReturnParcel' ? ACTION_TYPE.RETURN_PARCEL
    : item.action === 'processReturnZoneMove' ? ACTION_TYPE.RETURN_ZONE_MOVE
    : null;
```

(b) `alreadyProcessed` 처리(즉 `if (alreadyProcessed) { ... return {..., status: 'duplicate'}; }`) 블록 바로 다음, `photoUrl` 변수를 선언하는 줄 이전에 아래 블록을 삽입한다:
```js
  if (actionType === ACTION_TYPE.RETURN_ZONE_MOVE) {
    var stagingUpdate = buildStagingUpdate(timestamp);
    writeStagingResult_(rowIndex, stagingUpdate);
    appendLogRow_(buildLogRow({
      timestamp: timestamp,
      statusLabel: '회송존 이동',
      iprBarcode: item.iprBarcode,
      workerName: item.worker && item.worker.name,
      vfId: item.worker && item.worker.vfId,
      remark: '(오프라인 동기화)'
    }));
    return { iprBarcode: item.iprBarcode, status: 'applied' };
  }
```
(이 블록이 먼저 return하므로, 아래의 `buildProcessUpdate`/`writeProcessResult_`를 쓰는 기존 로직은 `processReturnZoneMove`가 아닌 나머지 3개 액션에만 적용된다.)

- [ ] **Step 3: Code.gs 라우팅 추가**

`appsscript/Code.gs`의 `switch (payload.action)` 안, `processReturnParcel` case 다음(`syncDown` case 앞이든 뒤든 상관없음, `default` 앞이기만 하면 됨)에 추가:
```js
      case 'processReturnZoneMove':
        result = handleProcessReturnZoneMove(payload);
        break;
```

- [ ] **Step 4: 수동 검증(코드 읽기)**

수정한 `appsscript/Handlers.gs` 전체를 다시 읽어 다음을 확인한다: `applySyncItem_`의 새 분기가 문법적으로 올바르게 삽입되었는지(중괄호 짝, 세미콜론), `buildStagingUpdate`/`writeStagingResult_`/`ACTION_TYPE.RETURN_ZONE_MOVE`가 각각 `Logic.js`/`Sheet.gs`/`Logic.js`에 실제로 존재하는 이름과 정확히 일치하는지.

- [ ] **Step 5: Commit**

```bash
git add appsscript/Handlers.gs appsscript/Code.gs
git commit -m "feat(appsscript): add processReturnZoneMove handler and sync-item support"
```

---

### Task 4: 프론트엔드 — 바코드 뒷자리 분리 (format.js, TDD)

**Files:**
- Modify: `frontend/js/lib/format.js`
- Modify: `frontend/js/lib/format.test.js`

- [ ] **Step 1: 실패하는 테스트 추가**

`frontend/js/lib/format.test.js`의 import 목록에 `splitBarcodeSuffix`를 추가하고(`import { formatDateDisplay, formatDPlus6Badge, formatMethodLabel, splitBarcodeSuffix } from './format.js';`), 파일 끝에 추가:
```js
test('splitBarcodeSuffix: 길이가 suffixLength보다 길면 뒤 자리를 분리한다', () => {
  assert.deepEqual(splitBarcodeSuffix('S0037699586659', 4), { prefix: 'S003769958', suffix: '6659' });
});

test('splitBarcodeSuffix: suffixLength 생략 시 기본값 4를 사용한다', () => {
  assert.deepEqual(splitBarcodeSuffix('ABCDEFGH'), { prefix: 'ABCD', suffix: 'EFGH' });
});

test('splitBarcodeSuffix: 길이가 suffixLength 이하이면 전체를 suffix로 반환한다', () => {
  assert.deepEqual(splitBarcodeSuffix('AB', 4), { prefix: '', suffix: 'AB' });
  assert.deepEqual(splitBarcodeSuffix('', 4), { prefix: '', suffix: '' });
});

test('splitBarcodeSuffix: null/undefined은 빈 문자열로 처리한다', () => {
  assert.deepEqual(splitBarcodeSuffix(null), { prefix: '', suffix: '' });
  assert.deepEqual(splitBarcodeSuffix(undefined), { prefix: '', suffix: '' });
});
```

- [ ] **Step 2: 테스트 실행하여 실패 확인**

Run: `cd frontend && npm test`
Expected: `splitBarcodeSuffix is not a function` 등으로 FAIL

- [ ] **Step 3: 구현 작성**

`frontend/js/lib/format.js` 파일 끝에 추가:
```js
export function splitBarcodeSuffix(barcode, suffixLength) {
  var str = String(barcode == null ? '' : barcode);
  var len = suffixLength || 4;
  if (str.length <= len) {
    return { prefix: '', suffix: str };
  }
  return { prefix: str.slice(0, str.length - len), suffix: str.slice(str.length - len) };
}
```

- [ ] **Step 4: 테스트 실행하여 통과 확인**

Run: `cd frontend && npm test`
Expected: `# fail 0`

- [ ] **Step 5: Commit**

```bash
git add frontend/js/lib/format.js frontend/js/lib/format.test.js
git commit -m "feat(frontend): add barcode suffix splitting for highlight display"
```

---

### Task 5: 프론트엔드 — 회송존 이동 payload 빌더 (api-payloads.js, TDD)

**Files:**
- Modify: `frontend/js/lib/api-payloads.js`
- Modify: `frontend/js/lib/api-payloads.test.js`

- [ ] **Step 1: 실패하는 테스트 추가**

`frontend/js/lib/api-payloads.test.js`의 import 목록에 `buildReturnZoneMovePayload`를 추가하고, 파일 끝에 추가:
```js
test('buildReturnZoneMovePayload', () => {
  const worker = { name: '홍길동', vfId: 'VF1' };
  assert.deepEqual(buildReturnZoneMovePayload('IPR1', worker), {
    action: 'processReturnZoneMove',
    iprBarcode: 'IPR1',
    worker: worker
  });
});
```

- [ ] **Step 2: 테스트 실행하여 실패 확인**

Run: `cd frontend && npm test`
Expected: `buildReturnZoneMovePayload is not a function` 등으로 FAIL

- [ ] **Step 3: 구현 작성**

`frontend/js/lib/api-payloads.js` 파일 끝에 추가:
```js
export function buildReturnZoneMovePayload(iprBarcode, worker) {
  return { action: 'processReturnZoneMove', iprBarcode: iprBarcode, worker: worker };
}
```

- [ ] **Step 4: 테스트 실행하여 통과 확인**

Run: `cd frontend && npm test`
Expected: `# fail 0`

- [ ] **Step 5: Commit**

```bash
git add frontend/js/lib/api-payloads.js frontend/js/lib/api-payloads.test.js
git commit -m "feat(frontend): add processReturnZoneMove payload builder"
```

---

### Task 6: 프론트엔드 — 화면 개명 + 뒤로가기 이벤트 (state.js, TDD)

**Files:**
- Modify: `frontend/js/lib/state.js`
- Modify: `frontend/js/lib/state.test.js`

`SCREEN.RETURN_VENDOR_CONFIRM`을 `SCREEN.RETURN_VENDOR_CHOICE`로 개명한다(업체직접회수 화면이 이제 단일 완료 버튼이 아니라 "회송존 이동"/"회수 완료" 두 선택지를 고르는 화면이 되므로). 이 태스크에서는 `state.js`/`state.test.js`만 고친다 — `app.js`/`returnVendor.js`는 Task 10에서 함께 갱신한다(그 전까지 앱을 브라우저로 실행하면 해당 화면이 깨질 수 있으나, Node 테스트에는 영향 없다).

- [ ] **Step 1: 실패하는 테스트로 개명**

`frontend/js/lib/state.test.js`를 열어, `CHOOSE_RETURN_METHOD` 관련 테스트에서 `SCREEN.RETURN_VENDOR_CONFIRM`을 참조하는 부분을 찾아 `SCREEN.RETURN_VENDOR_CHOICE`로 바꾼다(현재 테스트 이름은 `'CHOOSE_RETURN_METHOD: route가 parcel이면 택배 스캔 화면, vendor면 업체 확인 화면으로 이동한다'`이며, `assert.equal(reduce(initialState(), { type: 'CHOOSE_RETURN_METHOD', route: 'vendor' }).screen, SCREEN.RETURN_VENDOR_CONFIRM);` 형태의 라인이 있을 것이다 — 오른쪽 값만 `SCREEN.RETURN_VENDOR_CHOICE`로 바꾼다).

파일 끝에 `GO_BACK` 테스트를 추가한다:
```js
test('GO_BACK: 지정된 화면으로 이동하고 조회 결과는 유지한다', () => {
  const withInquiry = reduce(initialState(), { type: 'INQUIRY_RESULT', result: { found: true } });
  const discardScreen = reduce(withInquiry, { type: 'SELECT_DISCARD' });
  const state = reduce(discardScreen, { type: 'GO_BACK', screen: SCREEN.SCAN });
  assert.equal(state.screen, SCREEN.SCAN);
  assert.deepEqual(state.inquiry, { found: true });
});

test('GO_BACK: 메시지를 비운다', () => {
  const withMessage = reduce(initialState(), { type: 'PROCESS_ERROR', text: '오류' });
  const state = reduce(withMessage, { type: 'GO_BACK', screen: SCREEN.SCAN });
  assert.equal(state.message, null);
});
```

- [ ] **Step 2: 테스트 실행하여 실패 확인**

Run: `cd frontend && npm test`
Expected: `SCREEN.RETURN_VENDOR_CHOICE`가 `undefined`이거나 화면 비교 실패, `GO_BACK` 처리 안 됨(기본 분기라 상태가 그대로라 화면 불일치)으로 FAIL

- [ ] **Step 3: 구현 수정**

`frontend/js/lib/state.js`를 연다. `SCREEN` 객체에서 `RETURN_VENDOR_CONFIRM: 'RETURN_VENDOR_CONFIRM'` 줄을 `RETURN_VENDOR_CHOICE: 'RETURN_VENDOR_CHOICE'`로 바꾼다.

`reduce` 함수의 `CHOOSE_RETURN_METHOD` case에서 `SCREEN.RETURN_VENDOR_CONFIRM`을 참조하는 부분을 `SCREEN.RETURN_VENDOR_CHOICE`로 바꾼다.

`reduce` 함수의 `switch` 안, `case 'RESET_TO_SCAN':` 바로 위나 아래(어디든 다른 case 사이)에 추가:
```js
    case 'GO_BACK':
      return Object.assign({}, state, { screen: event.screen, message: null });
```

- [ ] **Step 4: 테스트 실행하여 통과 확인**

Run: `cd frontend && npm test`
Expected: `# fail 0`

- [ ] **Step 5: Commit**

```bash
git add frontend/js/lib/state.js frontend/js/lib/state.test.js
git commit -m "feat(frontend): rename vendor-confirm screen to vendor-choice, add GO_BACK event"
```

---

### Task 7: 프론트엔드 — processReturnZoneMove API 연동 (api.js, sync.js)

**Files:**
- Modify: `frontend/js/api.js` (주의: 아래 특별 지침 참고)
- Modify: `frontend/js/sync.js`

**중요 — api.js 취급 주의사항:** `frontend/js/api.js`에는 사용자가 실제 배포에 쓰는 `MOCK_MODE`/`API_BASE_URL` 값이 커밋되지 않은 채로 로컬에만 남아있을 수 있다. 이 태스크를 시작하기 전에 반드시 `git diff frontend/js/api.js`로 현재 상태를 확인한다.

- 만약 `git status`에 `frontend/js/api.js`가 이미 수정된 상태로 나온다면(즉 커밋되지 않은 로컬 변경이 있다면): 그 파일의 `MOCK_MODE`/`API_BASE_URL` 값을 기억해 둔 뒤, 새 함수를 추가하고 나서 커밋 직전에 `MOCK_MODE`를 `true`로, `API_BASE_URL`을 `'https://script.google.com/macros/s/REPLACE_WITH_DEPLOYMENT_ID/exec'`로 **일시적으로** 바꿔서 그 상태로만 커밋한 뒤, 커밋 직후 원래 로컬 값으로 다시 되돌려 놓는다(다시 되돌린 상태는 커밋하지 않는다 — `git add`하지 않은 채로 작업 트리에만 남겨둔다).
- 만약 `frontend/js/api.js`가 깨끗한 상태(수정 없음)라면 이 특별 절차는 필요 없다 — 그냥 평소처럼 수정하고 커밋한다.

- [ ] **Step 1: api.js에 processReturnZoneMove 추가**

`frontend/js/api.js`를 연다. 상단 import 줄(`import { buildInquiryPayload, buildDiscardPayload, buildReturnVendorPayload, buildReturnParcelPayload } from './lib/api-payloads.js';`)에 `buildReturnZoneMovePayload`를 추가한다.

`processReturnParcel` 함수 아래에 추가:
```js
export function processReturnZoneMove(iprBarcode, worker) {
  return callApi('processReturnZoneMove', buildReturnZoneMovePayload(iprBarcode, worker));
}
```

- [ ] **Step 2: sync.js에 오프라인 대응 추가**

`frontend/js/sync.js`를 연다. `processReturnParcel` 함수 아래에 추가:
```js
export function processReturnZoneMove(iprBarcode, worker) {
  return api.processReturnZoneMove(iprBarcode, worker).catch(function (err) {
    console.warn('온라인 회송존 이동 처리 실패 - 오프라인 큐에 저장:', err);
    return queueAndSucceed_('processReturnZoneMove', { iprBarcode: iprBarcode, worker: worker });
  });
}
```

- [ ] **Step 3: 문법 확인 + 회귀 테스트**

Run:
```bash
node --check frontend/js/api.js
node --check frontend/js/sync.js
cd frontend && npm test
```
Expected: 문법 오류 없음, 기존 테스트 전부 유지(`# fail 0`)

- [ ] **Step 4: Commit (api.js 취급 주의사항 반드시 지킬 것)**

```bash
git add frontend/js/api.js frontend/js/sync.js
git commit -m "feat(frontend): add processReturnZoneMove online/offline client support"
```
커밋 후 `git status`로 `frontend/js/api.js`가 (로컬 실배포 설정이 있었다면) 다시 수정된 상태로 나오는지, 그리고 그 안의 `MOCK_MODE`/`API_BASE_URL`이 커밋 직전에 기억해둔 원래 값과 같은지 반드시 확인한다.

---

### Task 8: 프론트엔드 — 조회 카드 IPR바코드 표시 + 상품바코드 강조

**Files:**
- Modify: `frontend/js/screens/scan.js`
- Modify: `frontend/css/style.css`

- [ ] **Step 1: scan.js의 renderInquiryCard_ 수정**

`frontend/js/screens/scan.js`를 연다. 상단 import 줄(`import { formatDateDisplay, formatDPlus6Badge, formatMethodLabel } from '../lib/format.js';`)에 `splitBarcodeSuffix`를 추가한다.

`renderInquiryCard_` 함수 전체를 읽고, "정상" 케이스(마지막 `return (...)` 블록)를 다음으로 교체한다 — `if (!inquiry.found)`/`if (inquiry.duplicate)`/`if (inquiry.offlineUnknown)` 등 앞부분의 조기 반환들은 그대로 둔다:
```js
  var barcodeParts = splitBarcodeSuffix(inquiry.productBarcode, 4);
  return (
    '<div class="card card-inquiry">' +
    (inquiry.alreadyProcessed ? '<div class="badge badge-warning">이미 처리된 건입니다 (' + escapeHtml(inquiry.existingStatus) + ')</div>' : '') +
    '<div class="field"><span>IPR바코드</span><strong>' + escapeHtml(inquiry.iprBarcode) + '</strong></div>' +
    '<div class="field"><span>상품바코드</span><strong>' + escapeHtml(barcodeParts.prefix) + '<span class="barcode-highlight">' + escapeHtml(barcodeParts.suffix) + '</span></strong></div>' +
    '<div class="field"><span>상품명</span><strong>' + escapeHtml(inquiry.productName) + '</strong></div>' +
    '<div class="field"><span>오류신고일</span><strong>' + escapeHtml(formatDateDisplay(inquiry.reportDate)) + '</strong></div>' +
    '<div class="field"><span>벤더명</span><strong>' + escapeHtml(inquiry.vendor) + '</strong></div>' +
    '<div class="field"><span>수량</span><strong>' + escapeHtml(inquiry.qty) + '</strong></div>' +
    '<div class="field"><span>회수구분</span><strong>' + escapeHtml(formatMethodLabel(inquiry.method)) + '</strong></div>' +
    '<div class="field"><span>D+6 초과여부</span><strong>' + formatDPlus6Badge(inquiry.isOverDPlus6) + '</strong></div>' +
    '<div class="action-row">' +
    '  <button id="discardBtn" class="btn btn-discard" type="button">폐기 처리</button>' +
    '  <button id="returnBtn" class="btn btn-return" type="button">회송 처리</button>' +
    '</div>' +
    '</div>'
  );
```
(기존에 있던 "상품바코드" 필드 한 줄은 위 두 번째 `<div class="field">`로 대체되고, 그 앞에 "IPR바코드" 필드가 새로 추가된 형태다. 나머지 필드는 순서와 내용 그대로 유지한다.)

- [ ] **Step 2: CSS 추가**

`frontend/css/style.css`를 연다. `.manual-input-row input{...}` 규칙 다음 줄에 추가:
```css
.barcode-highlight{color:var(--lotte-red);font-weight:900;font-size:1.1em;}
```

- [ ] **Step 3: 문법 확인**

Run: `node --check frontend/js/screens/scan.js`
Expected: 에러 없음

- [ ] **Step 4: Commit**

```bash
git add frontend/js/screens/scan.js frontend/css/style.css
git commit -m "feat(frontend): show IPR barcode and highlight product barcode suffix on inquiry card"
```

---

### Task 9: 프론트엔드 — 처리 화면 뒤로가기 전환 (discard/returnMethodChoice/returnParcel)

**Files:**
- Modify: `frontend/js/screens/discard.js`
- Modify: `frontend/js/screens/returnMethodChoice.js`
- Modify: `frontend/js/screens/returnParcel.js`

세 파일 모두 같은 패턴이다: 취소 버튼의 라벨을 "취소"에서 "뒤로가기"로 바꾸고, 클릭 시 `RESET_TO_SCAN`(조회 결과까지 지우고 완전히 초기화) 대신 `GO_BACK`(조회 결과는 유지한 채 지정된 화면으로만 이동)을 dispatch하도록 바꾼다. 세 파일 다 이미 `import { SCREEN } from '../lib/state.js';` 줄이 없다면 새로 추가해야 한다.

- [ ] **Step 1: discard.js 수정**

`frontend/js/screens/discard.js`를 읽는다. 파일 최상단에 `import { SCREEN } from '../lib/state.js';`를 추가한다(다른 import 줄 옆에).

`innerHTML` 템플릿 안에서 `id="discardCancel"`을 가진 버튼의 표시 텍스트(현재 `취소`)를 `뒤로가기`로 바꾼다.

`root.querySelector('#discardCancel').addEventListener('click', ...)` 핸들러의 본문을 찾아, 현재 `ctx.dispatch({ type: 'RESET_TO_SCAN' });` 형태로 되어 있는 부분을 다음으로 바꾼다:
```js
    ctx.dispatch({ type: 'GO_BACK', screen: SCREEN.SCAN });
```
(핸들러를 감싸는 `addEventListener(...)` 구조 자체는 그대로 둔다.)

- [ ] **Step 2: returnMethodChoice.js 수정**

`frontend/js/screens/returnMethodChoice.js`를 읽는다. 최상단에 `import { SCREEN } from '../lib/state.js';`를 추가한다.

`id="returnMethodCancel"` 버튼의 텍스트를 `취소`에서 `뒤로가기`로 바꾼다.

`root.querySelector('#returnMethodCancel').addEventListener('click', ...)` 핸들러 본문의 `ctx.dispatch({ type: 'RESET_TO_SCAN' });`을 다음으로 바꾼다:
```js
    ctx.dispatch({ type: 'GO_BACK', screen: SCREEN.SCAN });
```

- [ ] **Step 3: returnParcel.js 수정**

`frontend/js/screens/returnParcel.js`를 읽는다. 최상단에 `import { SCREEN } from '../lib/state.js';`를 추가한다.

`id="returnParcelCancel"` 버튼의 텍스트를 `취소`에서 `뒤로가기`로 바꾼다.

`root.querySelector('#returnParcelCancel').addEventListener('click', ...)` 핸들러 본문의 `ctx.dispatch({ type: 'RESET_TO_SCAN' });`을 다음으로 바꾼다(택배 회송 스캔 화면은 회송 방식 선택 화면의 하위 화면이므로, 완전 초기화가 아니라 그 화면으로 돌아간다):
```js
    ctx.dispatch({ type: 'GO_BACK', screen: SCREEN.RETURN_METHOD_CHOICE });
```

- [ ] **Step 4: 문법 확인**

Run:
```bash
node --check frontend/js/screens/discard.js
node --check frontend/js/screens/returnMethodChoice.js
node --check frontend/js/screens/returnParcel.js
```
Expected: 에러 없음

- [ ] **Step 5: Commit**

```bash
git add frontend/js/screens/discard.js frontend/js/screens/returnMethodChoice.js frontend/js/screens/returnParcel.js
git commit -m "feat(frontend): replace full-reset cancel with GO_BACK on process screens"
```

---

### Task 10: 프론트엔드 — 업체직접회수 2분기 재작성 + app.js 라우팅 + 브라우저 검증

**Files:**
- Modify: `frontend/js/screens/returnVendor.js` (전면 재작성)
- Modify: `frontend/js/app.js`

- [ ] **Step 1: returnVendor.js 전면 재작성**

`frontend/js/screens/returnVendor.js`의 전체 내용을 다음으로 교체한다:
```js
import { escapeHtml } from '../lib/html.js';
import { SCREEN } from '../lib/state.js';

export function renderReturnVendor(root, ctx) {
  var inquiry = ctx.state.inquiry;
  root.innerHTML =
    '<div class="card card-return">' +
    '  <h2>업체직접회수 처리</h2>' +
    '  <p>IPR: ' + escapeHtml(inquiry.iprBarcode) + '</p>' +
    '  <p>' + escapeHtml(inquiry.productName) + ' / ' + escapeHtml(inquiry.vendor) + ' / 수량 ' + escapeHtml(inquiry.qty) + '</p>' +
    '  <div class="action-row">' +
    '    <button id="zoneMoveBtn" class="btn btn-return" type="button">회송존 이동</button>' +
    '    <button id="pickupCompleteBtn" class="btn btn-return" type="button">회수 완료</button>' +
    '  </div>' +
    '  <button id="returnVendorBack" class="btn btn-secondary" type="button">뒤로가기</button>' +
    '</div>';

  root.querySelector('#zoneMoveBtn').addEventListener('click', function () {
    if (!window.confirm('회송존 이동으로 처리하시겠습니까?')) return;
    var btn = root.querySelector('#zoneMoveBtn');
    btn.disabled = true;
    ctx.sync.processReturnZoneMove(inquiry.iprBarcode, ctx.state.worker).then(function (res) {
      if (res.success) {
        var suffix = res.offline ? ' (오프라인 처리 — 동기화 대기 중)' : '';
        ctx.dispatch({ type: 'PROCESS_SUCCESS', text: '회송존 이동 처리가 완료되었습니다.' + suffix });
      } else {
        btn.disabled = false;
        ctx.dispatch({ type: 'PROCESS_ERROR', text: '회송존 이동 처리에 실패했습니다: ' + (res.error || '') });
      }
    });
  });

  root.querySelector('#pickupCompleteBtn').addEventListener('click', function () {
    if (!window.confirm('회수 완료로 처리하시겠습니까?')) return;
    var btn = root.querySelector('#pickupCompleteBtn');
    btn.disabled = true;
    ctx.sync.processReturnVendor(inquiry.iprBarcode, ctx.state.worker).then(function (res) {
      if (res.success) {
        var suffix = res.offline ? ' (오프라인 처리 — 동기화 대기 중)' : '';
        ctx.dispatch({ type: 'PROCESS_SUCCESS', text: '업체 트럭 회송 처리가 완료되었습니다.' + suffix });
      } else {
        btn.disabled = false;
        ctx.dispatch({ type: 'PROCESS_ERROR', text: '회송 처리에 실패했습니다: ' + (res.error || '') });
      }
    });
  });

  root.querySelector('#returnVendorBack').addEventListener('click', function () {
    ctx.dispatch({ type: 'GO_BACK', screen: SCREEN.RETURN_METHOD_CHOICE });
  });
}
```

- [ ] **Step 2: app.js 라우팅 갱신**

`frontend/js/app.js`를 읽고, `switch (state.screen)` 안에서 `case SCREEN.RETURN_VENDOR_CONFIRM: renderReturnVendor(root, ctx); break;`로 되어 있는 줄을 찾아 다음으로 바꾼다:
```js
    case SCREEN.RETURN_VENDOR_CHOICE: renderReturnVendor(root, ctx); break;
```
(다른 case들과 파일의 나머지 부분은 그대로 둔다.)

- [ ] **Step 3: 문법 확인 + 회귀 테스트**

Run:
```bash
node --check frontend/js/screens/returnVendor.js
node --check frontend/js/app.js
cd frontend && npm test
```
Expected: 에러 없음, 기존 테스트 전부 유지(`# fail 0`)

- [ ] **Step 4: Commit**

```bash
git add frontend/js/screens/returnVendor.js frontend/js/app.js
git commit -m "feat(frontend): split vendor return into zone-move/pickup-complete with confirm dialogs"
```

- [ ] **Step 5: 브라우저 수동 검증 (Mock 모드)**

`frontend/js/api.js`의 `MOCK_MODE`가 로컬에서 어떤 값인지 먼저 `git diff frontend/js/api.js`로 확인한다. 검증에는 `MOCK_MODE = true`가 필요하다 — 현재 `false`(실배포 URL)로 되어 있다면 검증 동안만 `true`로 바꾸고, 끝나면 정확히 원래 값으로 되돌린다(이 파일은 이 태스크에서도 커밋하지 않는다 — Task 7에서 이미 커밋 처리를 끝냈다).

정적 서버를 띄우고(기존에 떠 있으면 재사용) 브라우저에서 로그인 후 아래를 확인한다:

1. **IPR바코드 표시 + 강조**: `IPR0001`(Mock 데이터, 상품바코드 `S0000001`) 조회 → 카드 맨 위에 "IPR바코드: IPR0001"이 보이고, "상품바코드" 필드에서 뒤 4자리(`0001`)가 시각적으로 강조(다른 색/굵게)되어 있는지 확인.
2. **폐기 화면 뒤로가기**: `IPR0001` 조회 → [폐기 처리] → 폐기존 이동 후 사진 화면에서 [뒤로가기] 클릭 → 스캔 대기 화면으로 돌아가되, 방금 조회했던 `IPR0001` 카드가 다시 보이고 [폐기 처리]/[회송 처리] 버튼도 다시 뜨는지 확인(재스캔 없이).
3. **회송 방식 선택 뒤로가기**: `IPR0001` 조회 → [회송 처리] → 회송 방식 선택 화면에서 [뒤로가기] → 마찬가지로 조회 카드가 유지된 채 스캔 대기 화면으로 복귀하는지 확인.
4. **택배 스캔 뒤로가기**: `IPR0001` 조회 → [회송 처리] → [택배] → 운송장 스캔 화면에서 [뒤로가기] → 회송 방식 선택 화면(택배/업체직접회수 버튼)으로 돌아가는지 확인(스캔 대기 화면이 아님).
5. **업체직접회수 2분기 + 재확인**: `IPR0002`(Mock 데이터, 업체직접회수) 조회 → [회송 처리] → [업체직접회수] → "업체직접회수 처리" 화면에 [회송존 이동]/[회수 완료]/[뒤로가기] 3개 버튼이 보이는지 확인.
   - [회송존 이동] 클릭 → 네이티브 확인창이 뜨는지 확인 → 취소를 누르면 아무 일도 안 일어나는지 확인 → 다시 클릭해 이번엔 확인을 누르면 "회송존 이동 처리가 완료되었습니다." 메시지와 함께 스캔 대기 화면으로 돌아가는지 확인.
   - 다시 `IPR0002`를 조회하고 [회송 처리] → [업체직접회수] → [회수 완료] 클릭 → 확인창에서 확인 → "업체 트럭 회송 처리가 완료되었습니다." 메시지 확인.
   - 이 화면의 [뒤로가기]는 회송 방식 선택 화면으로 돌아가는지 확인.
6. **콘솔 에러 없음**을 `read_console_messages`(`onlyErrors: true`)로 확인.

문제가 있으면 수정 후 재검증하고, 필요하면 별도 커밋한다(`frontend/js/api.js`는 절대 포함하지 않는다):
```bash
git add -A -- ':!frontend/js/api.js'
git commit -m "fix(frontend): address issues found in v3.1 manual verification"
```

---

### Task 11: 문서 업데이트

**Files:**
- Modify: `docs/로컬-테스트-가이드.md`

- [ ] **Step 1: 테스트 절 추가**

`docs/로컬-테스트-가이드.md`를 읽는다. 현재 마지막 섹션(Node 단위 테스트 실행) 앞에 새 섹션을 추가한다(번호는 현재 문서의 마지막 섹션 번호에 맞춰 자연스럽게 매긴다):
```markdown
## 오입력 방지 기능 확인하기 (v3.1)

1. 아무 IPR바코드나 조회한 뒤 [폐기 처리] 또는 [회송 처리]로 들어가서 [뒤로가기]를 눌러보면, 재스캔 없이 방금 조회했던 카드로 바로 돌아오는지 확인한다.
2. 조회 카드 맨 위에 IPR바코드가 뜨는지, 상품바코드 뒤 4자리가 강조 표시되는지 확인한다.
3. [회송 처리] → [업체직접회수]에서 [회송존 이동]/[회수 완료] 각각을 눌러보고, 확인창이 뜨는지, 취소를 누르면 아무 처리도 안 되는지, 확인을 누르면 실제로 처리되는지 확인한다. 실제 시트 연동 중이라면 [회송존 이동]은 U열/X열만 채워지고 S/T열은 비어있는 채로 남는지, [회수 완료]는 기존처럼 S/T열이 채워지는지 확인한다.
```
기존 섹션들의 번호는 건드리지 않는다(새 섹션은 번호 없이 추가하거나, 마지막 섹션 앞에 자연스러운 순번으로 끼워 넣는다 — 어느 쪽이든 문서가 읽히는 데 지장 없으면 된다).

- [ ] **Step 2: Commit**

```bash
git add docs/로컬-테스트-가이드.md
git commit -m "docs: add v3.1 manual testing steps to local testing guide"
```

---

## Self-Review 체크리스트

- 설계 문서 10장 4개 요구사항 반영 확인: 뒤로가기(Task 6, 9, 10) / IPR바코드 표시(Task 8) / 상품바코드 뒷자리 강조(Task 4, 8) / 업체직접회수 2분기+재확인(Task 1~3, 5, 7, 10) — 전부 태스크로 커버됨.
- 플레이스홀더 없음(모든 스텝에 실제 코드 포함).
- 함수/타입 이름 일관성: `COLUMNS.PROGRESS_STATUS`, `ACTION_TYPE.RETURN_ZONE_MOVE`, `buildStagingUpdate`, `writeStagingResult_`, `handleProcessReturnZoneMove`가 appsscript 쪽 Task 1~3에서, `splitBarcodeSuffix`, `buildReturnZoneMovePayload`, `SCREEN.RETURN_VENDOR_CHOICE`, `GO_BACK`, `processReturnZoneMove`(api.js/sync.js)가 frontend 쪽 Task 4~10에서 동일한 이름으로 일관되게 사용됨을 확인.
- `api.js`를 건드리는 유일한 태스크(Task 7)에 사용자의 로컬 실배포 설정을 보존하는 절차를 명시함(Task 5~6의 과거 리뷰에서 실제로 이 부분이 누락되어 커밋 완전성 문제가 있었던 전례 있음 — 이번엔 절차를 태스크 안에 명시적으로 포함).
